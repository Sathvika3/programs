lass Solution{
    int middle(int A, int B, int C){
        //code here
        if((A<B && A>C) || (A>B && A<C))
            return A;
        else if((B>C) && (B<A) ||(B>A) && (B<C))
            return B;
        else 
            return C;
    }
}
